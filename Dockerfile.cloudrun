FROM python:3.11-slim

WORKDIR /app

COPY requirements-cloudrun.txt .
RUN pip install --no-cache-dir -r requirements-cloudrun.txt

COPY app/ app/

CMD ["sh", "-c", "uvicorn app.main_cloudrun:app --host 0.0.0.0 --port ${PORT:-8080}"]
